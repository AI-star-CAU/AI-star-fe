# FE 수정 제안서 — Phase 2/3/4 API 명세 정합성

**작성일:** 2026-05-26  
**기준 명세:** Phase 2 v0.4 / Phase 3 v0.8 / Phase 4 v0.6  
**상태:** Phase 4 구현 전 설계 단계 — 코드 미수정

---

## 목차

1. [FE 반영 사항](#1-fe-반영-사항)
2. [BE 수정·확인 요청](#2-be-수정확인-요청)
3. [Phase 4 파일 구조 설계안](#3-phase-4-파일-구조-설계안)

---

## 1. FE 반영 사항

### Phase 3 미완성 항목

---

#### F3-1. `useUpdateBranch` 훅 부재

**명세:** Phase 3 §2.2 `PATCH /api/v1/chats/{chatId}` — 분기 제목 사용자 수정

**현황:**  
`branchApi.updateBranch()` 함수(`features/branch/api/branchApi.ts:13`)는 구현돼 있으나 React 훅이 없어 UI에서 호출 불가. `features/branch/hooks/`에 `useCreateBranch.ts`는 있지만 `useUpdateBranch.ts`가 없음.

**작업:**
- `features/branch/hooks/useUpdateBranch.ts` 신설
- mutation 성공 시 `['graph']`, `['conversations']` invalidate + toast

---

#### F3-2. `summaryStatus: PENDING` 상태 UI 없음 (낮은 우선순위)

**명세:** Phase 3 §3.1 TurnNodeDto — `summaryStatus: "PENDING" | "GENERATED"`

**현황:**  
`useGraph`는 PENDING일 때 3초 폴링을 구현하나, 그래프 노드 UI에서 summary가 null일 때 "생성 중" 표시가 없음. `GraphPanel.tsx` 내에서 실제 렌더링 확인 후 판단.

---

### Phase 4 미구현 항목

---

#### F4-1. 대화 탐색기 API 미연동 ⭐ 최우선

**명세:** Phase 4 §2.1 `GET /api/v1/chats/explorer`

**현황:**  
`features/conversation-explorer/hooks/useConversations.ts`가 Phase 2 `GET /chats`를 호출.  
Phase 4는 응답 구조가 완전히 다름 — 분기 트리를 평탄화 배열로 함께 반환.

**Phase 2 vs Phase 4 응답 비교:**

| 항목 | Phase 2 `GET /chats` | Phase 4 `GET /chats/explorer` |
|---|---|---|
| 루트 chat 목록 | `content[]` flat | `roots[].nodes[]` 트리 |
| 분기 포함 여부 | 없음 (`branches: []` 고정) | `depth`, `parentChatId`로 포함 |
| 시각 필드 | `lastMessageAt` (nullable) | `lastActivityAt` (NOT NULL) |
| preview | `lastMessagePreview` | 없음 (graph의 summary 활용) |
| 페이징 | `totalElements`, `totalPages` | `totalRootCount`, `hasNext` |

**신설·수정할 파일** → [§3 파일 구조 설계안](#3-phase-4-파일-구조-설계안) 참조

---

#### F4-2. 토큰 사용량 API 미연동 ⭐ 최우선

**명세:** Phase 4 §3.3 `GET /api/v1/usage/me`

**현황:**  
`features/member/components/UsageMeter.tsx`가 하드코딩 데이터 사용:
```tsx
// 현재
plan === 'free' → "12,450 / 50,000" 고정
plan === 'pro'  → "무제한" 고정
```

**명세 응답 필드:**
```
tokensUsed, tokenLimit, remainingTokens
usageRatio (0~1), warningLevel ("NONE"|"WARN"|"CRITICAL")
planName, periodStart, periodEnd, requestCount
```

**주의사항:**
- `tokenLimit === 0` → 무제한 plan → `remainingTokens: null`, `warningLevel: "NONE"`
- `warningLevel` 기준: 80% 이상 `WARN`, 95% 이상 `CRITICAL`

**신설·수정할 파일** → [§3 파일 구조 설계안](#3-phase-4-파일-구조-설계안) 참조

---

#### F4-3. `TurnCompletedData` Phase 4 필드 누락

**명세:** Phase 4 §3.4 — `turn_completed` SSE 스키마 확장

**현황** (`features/chat/api/streamTypes.ts:23`):
```typescript
export interface TurnCompletedData {
  turnId: number;
  aiMessageId: number;
  answerToken: number;
  summaryStatus: 'PENDING';
  // 아래 3개 없음
}
```

**추가 필요:**
```typescript
contextTokens?: number;        // 압축 후 LLM에 전달된 실제 입력 토큰 수
compressionApplied?: boolean;  // 압축 1턴 이상 적용 여부
compressedTurnCount?: number;  // 압축 turn 수 (summary 치환 + truncation 합산)
```
> optional(`?`)로 처리하면 Phase 2/3 응답과 하위 호환. BE에 항상 전송하는지 확인 필요 → [§2 BE-3](#be-3-phase-4-turn_completed-신규-필드-optional-여부).

---

#### F4-4. 압축 발생 시 UI 알림 없음

**명세:** Phase 4 §3.2 — `compressionApplied: true`일 때 UI 안내 권장

**현황:** SSE 핸들러(`useSendMessage`, `useRegenerate`, `useEditMessage`)에서 `turn_completed` 이벤트를 받지만 `compressionApplied` 처리 없음.

**작업:** F4-3 완료 후 SSE 핸들러에 `compressionApplied` 확인 + toast/배너 표시 추가.  
메시지 예시: "이번 대화의 맥락이 모델 한도에 근접해 일부 내용이 요약됐어요. 분기를 만들어 흐름을 나누어 보세요."

---

#### F4-5. Phase 4 에러 코드 3개 누락

**파일:** `shared/api/errorCodes.ts`

추가 필요:
```typescript
EXPLORER_4001: '탐색기 조회 조건이 올바르지 않아요.',  // GET /chats/explorer 파라미터 오류
CONTEXT_4001:  '맥락 조회 조건이 올바르지 않아요.',
USAGE_4041:    '사용량 기록을 찾을 수 없어요.',         // GET /usage/me plan 없음
```

---

#### F4-6. Phase 4 엔드포인트 누락

**파일:** `shared/api/endpoints.ts`

추가 필요:
```typescript
explorer: {
  tree: '/chats/explorer',
  singleTree: (rootChatId: number) => `/chats/explorer/${rootChatId}`,
},
usage: {
  me: '/usage/me',
},
```

---

### 정리 필요 항목 (추후)

---

#### FC-1. `Conversation.branches: Branch[]` 제거 예정

**파일:** `features/chat/types.ts`, `features/chat/api/chatMappers.ts`

Phase 4 explorer API 이행 후 `Conversation.branches` 필드 제거. 현재는 `mapChatListItemToConversation`에서 항상 `branches: []`로 매핑 중. explorer API로 전환되면 분기 정보는 `ExplorerNodeDto`로 제공되므로 이 필드가 불필요해짐.

---

#### FC-2. `restoreBranch` 비명세 엔드포인트 처리

**파일:** `features/branch/api/branchApi.ts:23`, `shared/api/endpoints.ts`

`POST /chats/{id}/restore`는 Phase 2/3/4 어느 명세에도 없음. BE 확인 후 처리 → [§2 BE-4](#be-4-post-chatsidrrestore-엔드포인트-정의-요청).

---

### FE 반영 우선순위 요약

| 순서 | 항목 | 파일 | 규모 |
|---|---|---|---|
| 1 | F4-5 에러 코드 추가 | `shared/api/errorCodes.ts` | 3줄 |
| 2 | F4-6 엔드포인트 추가 | `shared/api/endpoints.ts` | 6줄 |
| 3 | F4-3 TurnCompletedData 필드 추가 | `chat/api/streamTypes.ts` | 3줄 |
| 4 | F3-1 useUpdateBranch 훅 신설 | `branch/hooks/` | 소 |
| 5 | F4-2 사용량 API 연동 | `member/` | 중 |
| 6 | F4-1 탐색기 API 전환 | `conversation-explorer/` | 대 |
| 7 | F4-4 압축 알림 UI | SSE 핸들러 | 소 |
| 추후 | FC-1 branches 제거 | types + mapper | 소 |
| BE확인후 | FC-2 restoreBranch 처리 | branch API | 소 |

---

## 2. BE 수정·확인 요청

> **대상:** BE 팀 / 명세서 작성자  
> 아래 항목에 대한 답변 또는 명세 보완이 있어야 FE 구현이 확정됩니다.

---

#### BE-1. `branch_created` SSE 이벤트 데이터 스키마 명세화 요청 🔴

**관련 명세:** Phase 3 §4.2 (사용자 메시지 수정)

Phase 3 §4.2에서 edit message의 SSE 흐름이 `branch_created → turn_started → ...` 순서임을 명시하고 있으나, `branch_created` 이벤트의 `data` 스키마가 명세에 없습니다.

FE가 추정 구현한 스키마:
```typescript
interface BranchCreatedData {
  newChatId: number;
  branchPointTurnId: number;
  title: string | null;
  titleStatus: 'PENDING' | 'GENERATED' | 'USER_EDITED';
}
```
**요청:** 위 스키마가 맞는지 확인하고 Phase 3 §4.2에 data 스키마를 추가해 주세요.

---

#### BE-2. send message SSE — `turn_completed.summary` 제거 시점 확인 🔴

**관련 명세:** Phase 3 §8 변경 이력 — "Phase 2의 일반 메시지 송신 API에도 후속 마이그레이션 권장"

Phase 3에서 `turn_completed`의 `summary` 필드 제거 + `summaryStatus: "PENDING"` 추가가 재생성·수정 SSE에 적용됐습니다. FE는 이미 send message SSE도 동일하게 반영했습니다.

**요청:** `POST /chats/{id}/messages`의 `turn_completed` 이벤트에서도 `summary` 필드가 제거됐는지 확인해 주세요. BE에 아직 `summary`가 남아 있다면 FE는 무시하고 처리하므로 동작에는 문제없지만, 명세 일관성을 위해 BE도 동일하게 수정 부탁드립니다.

---

#### BE-3. Phase 4 `turn_completed` 신규 필드 — optional 여부 🟠

**관련 명세:** Phase 4 §3.4

`contextTokens`, `compressionApplied`, `compressedTurnCount` 세 필드가 Phase 4 `turn_completed`에 추가됩니다.

**질문:** 압축이 발생하지 않은 경우에도 이 필드들이 **항상 전송**되나요? (`compressionApplied: false`, `compressedTurnCount: 0`)  
아니면 압축이 발생한 경우에만 포함되어 **없을 수 있나요**?

FE에서 TypeScript 타입 optional 여부(`?`)가 달라지므로 확인 부탁드립니다.

---

#### BE-4. `POST /chats/{id}/restore` 엔드포인트 정의 요청 🟠

**관련 명세:** Phase 4 §5.4 — "복구 API는 본 명세 범위 외"

FE 코드에 `branchApi.restoreBranch(chatId)`가 구현돼 있으며 `POST /chats/{id}/restore`를 호출합니다. 그러나 어떤 Phase 명세에도 이 경로가 없습니다.

**요청 (택 1):**
- **구현 예정이라면:** 경로·요청·응답 스키마를 명세에 추가해 주세요. FE가 맞게 구현돼 있는지 확인도 필요합니다.
- **구현 계획 없다면:** FE에서 해당 함수와 엔드포인트를 제거하겠습니다.

---

#### BE-5. `summaryStatus`가 영구 PENDING인 경우 처리 🟡

**관련 명세:** Phase 3 §0.5 원칙 3 — 비동기 summary 생성

**현황:** FE는 `summaryStatus: "PENDING"` 또는 `titleStatus: "PENDING"`인 노드가 있으면 3초 간격으로 무한 폴링합니다. 비동기 LLM 워커가 실패하면 PENDING이 영구 유지될 수 있습니다.

**질문:**
1. summary 생성 실패 시 별도 상태(`FAILED` 등)가 있나요, 아니면 PENDING 유지인가요?
2. 타임아웃(예: n분 내 미생성 시 포기)이 있나요?

답변이 "PENDING 유지"라면 FE에서 폴링 횟수 상한(예: 10회)을 자체적으로 설정하겠습니다.

---

#### BE-6. `GET /chats/explorer/{rootChatId}` 구현 여부 🟡

**관련 명세:** Phase 4 §2.2 (선택 구현)

분기 생성·삭제 후 해당 root tree만 새로고침하는 최적화 엔드포인트입니다.

**질문:** Phase 4 내에 구현 예정인가요? 구현한다면 FE도 이를 활용한 invalidation 최적화를 함께 구현하겠습니다. 구현하지 않더라도 전체 탐색기 재조회로 대응 가능합니다.

---

#### BE-7. Phase 4 탐색기 전환 계획 확인 🟡

**관련 명세:** Phase 4 §2.1

FE는 현재 Phase 2 `GET /chats`로 사이드바를 운영 중입니다. Phase 4 explorer API 전환 시 응답 구조가 완전히 달라집니다.

**질문:**
1. Phase 4 배포 후 Phase 2 `GET /chats`가 **유지**되나요, 아니면 **deprecated·제거** 예정인가요?
2. 두 API가 공존하는 전환 기간이 있다면 알려주세요. FE는 탐색기 API로 전환하는 시점을 BE 배포 일정에 맞춰 계획하겠습니다.

---

### BE 요청 요약

| # | 요청 | 우선순위 | 필요한 답변 |
|---|---|---|---|
| BE-1 | `branch_created` SSE 스키마 명세화 | 🔴 | 스키마 확인 및 §4.2 추가 |
| BE-2 | send message `turn_completed.summary` 제거 여부 | 🔴 | BE 현재 상태 확인 |
| BE-3 | Phase 4 `turn_completed` 필드 optional 여부 | 🟠 | optional/required 결정 |
| BE-4 | `restoreBranch` 엔드포인트 존재 여부 | 🟠 | 구현 계획 또는 제거 확정 |
| BE-5 | `summaryStatus` 영구 PENDING 처리 | 🟡 | 실패 상태 여부 |
| BE-6 | `GET /chats/explorer/{rootChatId}` 구현 여부 | 🟡 | 구현 여부 확인 |
| BE-7 | Phase 4 탐색기 전환 타임라인 | 🟡 | Phase 2 `GET /chats` 유지 기간 |

---

## 3. Phase 4 파일 구조 설계안

Phase 4 구현 시 추가·수정·삭제될 파일입니다. 현재 구조를 기준으로 변경 사항만 표기합니다.

```
src/
│
├── shared/
│   └── api/
│       ├── endpoints.ts          ✏️ explorer, usage 엔드포인트 추가
│       └── errorCodes.ts         ✏️ EXPLORER_4001, CONTEXT_4001, USAGE_4041 추가
│
├── features/
│   │
│   ├── branch/
│   │   └── hooks/
│   │       └── useUpdateBranch.ts  🆕 분기 제목 수정 mutation hook (Phase 3 완성)
│   │
│   ├── chat/
│   │   └── api/
│   │       └── streamTypes.ts      ✏️ TurnCompletedData에 Phase 4 필드 3개 추가
│   │
│   ├── conversation-explorer/      ← Phase 4에서 가장 큰 변경
│   │   ├── types.ts               🆕 ExplorerNodeDto, ExplorerTreeDto, ExplorerPageResponse
│   │   ├── api/                   🆕 (디렉터리 신설)
│   │   │   ├── explorerApi.ts     🆕 GET /chats/explorer, GET /chats/explorer/{rootChatId}
│   │   │   └── schemas.ts         🆕 Zod 스키마 (ExplorerNodeSchema 등)
│   │   ├── hooks/
│   │   │   ├── useConversations.ts  ✏️ → deprecated shim or 삭제
│   │   │   └── useExplorer.ts     🆕 탐색기 트리 조회 + titleStatus PENDING 폴링
│   │   └── components/
│   │       └── ConversationList.tsx ✏️ ExplorerNodeDto 기반 트리 렌더링으로 교체
│   │
│   └── member/
│       ├── types.ts               🆕 UsageResponse 타입 정의
│       ├── api/
│       │   ├── memberApi.ts       (유지)
│       │   └── usageApi.ts        🆕 GET /usage/me 호출
│       ├── hooks/                 🆕 (디렉터리 신설)
│       │   └── useUsage.ts        🆕 토큰 사용량 조회 + 캐시
│       └── components/
│           └── UsageMeter.tsx     ✏️ 실제 API 데이터 연동, warningLevel 색상 처리
```

### 신설 타입 정의 (설계안)

**`features/conversation-explorer/types.ts`**
```typescript
// Phase 4 §2.1
export interface ExplorerNodeDto {
  chatId: number;
  parentChatId: number | null;
  branchPointTurnId: number | null;
  title: string | null;               // null → UI에서 "제목없음" / "새 분기" 표시
  titleStatus: 'PENDING' | 'GENERATED' | 'USER_EDITED';
  depth: number;                      // 0 = root
  turnCount: number;
  lastActivityAt: string;             // NOT NULL
  createdAt: string;
  deletedAt: string | null;           // includeDeleted=true일 때 non-null 가능
  llmProvider: string;
  llmModel: string;
}

export interface ExplorerTreeDto {
  rootChatId: number;
  nodes: ExplorerNodeDto[];           // DFS 순서, 루트가 0번
}

export interface ExplorerPageResponse {
  roots: ExplorerTreeDto[];
  page: number;
  size: number;
  totalRootCount: number;
  hasNext: boolean;
}
```

**`features/member/types.ts`**
```typescript
// Phase 4 §3.3
export interface UsageResponse {
  usageRecordId: number;
  planId: number;
  planName: string;
  periodStart: string;
  periodEnd: string;
  tokensUsed: number;
  tokenLimit: number;                 // 0 = 무제한
  requestCount: number;
  remainingTokens: number | null;     // tokenLimit=0이면 null
  usageRatio: number | null;          // 0~1, tokenLimit=0이면 null
  warningLevel: 'NONE' | 'WARN' | 'CRITICAL';
}
```

### `useExplorer` 설계안

```typescript
// features/conversation-explorer/hooks/useExplorer.ts
// Phase 4 §2.1
// - titleStatus=PENDING인 노드가 있으면 3초 폴링 (useGraph와 동일 패턴)
// - sort, page, size 파라미터 지원
export const useExplorer = (params?: ExplorerQueryParams) => useQuery({ ... });
```

### `useUsage` 설계안

```typescript
// features/member/hooks/useUsage.ts
// Phase 4 §3.3
// - staleTime: 1분 (실시간 정확성보다 캐시 친화성 우선, 명세 §0.5 원칙 5)
// - SSE turn_completed의 contextTokens로 낙관적 업데이트 가능 (선택)
export const useUsage = () => useQuery({ ... });
```

### 기존 파일 변경 상세

| 파일 | 변경 내용 |
|---|---|
| `shared/api/endpoints.ts` | `explorer.tree`, `explorer.singleTree`, `usage.me` 추가 |
| `shared/api/errorCodes.ts` | `EXPLORER_4001`, `CONTEXT_4001`, `USAGE_4041` 추가 |
| `chat/api/streamTypes.ts` | `TurnCompletedData`에 `contextTokens?`, `compressionApplied?`, `compressedTurnCount?` 추가 |
| `conversation-explorer/hooks/useConversations.ts` | 탐색기 전환 후 `useExplorer`로 교체 또는 re-export shim |
| `conversation-explorer/components/ConversationList.tsx` | `ExplorerNodeDto` 기반 트리 렌더링 (depth로 들여쓰기) |
| `member/components/UsageMeter.tsx` | `UsageResponse` props로 교체, `warningLevel`별 색상 |

---

## 4. Phase 4 명세서(v0.6) 피드백

### 짚어야 할 이슈

#### P4F-1. `turn_completed` 신규 필드 전송 조건이 절들 사이에서 모순 🔴

같은 세 필드(`contextTokens`, `compressionApplied`, `compressedTurnCount`)를 두고 세 절이 서로 다르게 전제합니다.

- **§3.2 결과 통보:** "**압축이 적용된** LLM 호출에서는 ... 다음 필드를 포함한다" → 압축이 없으면 미포함으로 읽힘.
- **§3.4 스키마 표:** 세 필드를 optional 표기 없이 일반 필드로 나열 → 항상 포함으로 읽힘.
- **§5.3 시나리오:** "프론트는 **매** `turn_completed`의 `contextTokens`를 누적치에 더해 캐시" → 압축 여부와 무관하게 `contextTokens`가 항상 있어야 성립.

§3.2대로 압축 시에만 보내면 §5.3의 "매 턴 누적 캐시"가 불가능합니다. **권장:** 세 필드를 항상 전송하되 압축이 없으면 `compressionApplied:false`, `compressedTurnCount:0`, `contextTokens`는 실제 입력 토큰 수로 채우는 것으로 §3.2 문구를 정정하고 §3.4에 "항상 전송" 명시. (→ §2 BE-3 질문과 직결되며, 답이 정해지면 F4-3의 TypeScript optional 여부도 확정됩니다.)

#### P4F-2. `CONTEXT_4001` 에러 코드에 대응 엔드포인트가 없음 🟠

§0.2가 `CONTEXT_4001`("유효하지 않은 맥락 조회 파라미터")을 정의하지만, §3.1이 "맥락 조립은 서버 내부 동작이며 **별도의 외부 API는 없다**"고 못박고 있어 이 코드를 반환할 엔드포인트가 명세 어디에도 없습니다. (§2.1·§2.2는 `EXPLORER_4001`, §3.3은 `USAGE_4041`만 사용.)

FE는 §1의 F4-5에서 이 코드의 사용자 메시지를 추가하지만 **영원히 트리거되지 않는 dead 메시지**가 됩니다. **권장:** 향후 맥락 조회/디버그 엔드포인트 계획이 있으면 명세에 추가하고, 없으면 §0.2에서 `CONTEXT_4001`을 제거. (제거 시 FE도 F4-5에서 빼겠습니다.)

#### P4F-3. 빈 사용자·경계 응답이 미명시 🟡

`GET /chats/explorer` 호출자가 소유한 chat이 0개일 때 응답 형태(예상: `roots: [], totalRootCount: 0, hasNext: false`)가 명시되지 않았습니다. 탐색기가 사이드바 첫 진입의 기본 화면이라 FE는 이 빈 상태를 반드시 렌더링해야 합니다. 한 줄이라도 명시되면 좋겠습니다.

#### P4F-4. 사소 — 타입 표기 일관성 🟡

- §3.3 `usageRatio`만 타입이 `Number`로, 다른 필드의 `Long`/`Integer`/`DateTime` 표기 컨벤션(§0.1)과 어긋납니다. `Double` 또는 `Number(0~1)`로 통일 권장.
- `tokensUsed`/`tokenLimit`/`remainingTokens`가 `Long`입니다. JS `number`는 2^53까지 안전하므로 실사용 토큰 범위에서는 문제없으나, 추후 한도가 커질 경우를 대비해 인지만 해두면 됩니다.

### 요약

| # | 항목 | 우선순위 | 명세 측 조치 |
|---|---|---|---|
| P4F-1 | `turn_completed` 필드 전송 조건 §3.2↔§3.4↔§5.3 모순 | 🔴 | "항상 전송"으로 정정·명시 |
| P4F-2 | `CONTEXT_4001` 대응 엔드포인트 부재 | 🟠 | 엔드포인트 추가 또는 코드 제거 |
| P4F-3 | 빈 사용자 탐색기 응답 미명시 | 🟡 | 빈 상태 응답 1줄 명시 |
| P4F-4 | `usageRatio` 타입 표기 불일치 | 🟡 | `Double`로 통일 |
